# Performance baselines

`npm run bench:sim` writes local benchmark history to `bench-results/`, which is intentionally gitignored.

Commit only selected JSON snapshots here when they are useful as named reference baselines (for example before/after a major simulation change). Keep the original revision metadata in each snapshot.
